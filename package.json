{
  "name": "kimi-ai-web",
  "version": "1.0.0",
  "description": "Kimi AI Web Application with Firebase Hosting",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "mkdir -p www && cp *.html www/ && cp -r js/ www/ && cp -r css/ www/ && cp -r images/ www/ && cp -r data/ www/ && cp manifest.webmanifest www/ && cp sw.js www/ && cp -r functions/ www/functions/ || true",
    "serve": "firebase serve",
    "deploy": "firebase deploy",
    "emulators": "firebase emulators:start",
    "start": "npm run dev"
  },
  "keywords": [
    "kimi",
    "ai",
    "chatbot",
    "firebase"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/network": "^7.0.2",
    "cordova-plugin-network-information": "^3.0.0",
    "cordova-plugin-sim": "^1.3.3"
  }
}
